#include <stdio.h>

int main() {
    int i, anos, meses, dias; 
    scanf("%d", &i); 

    anos = i / 365; 
    i = i % 365;    

    meses = i / 30; 
    dias = i % 30;  

    
    printf("%d ano(s)\n", anos);
    printf("%d mes(es)\n", meses);
    printf("%d dia(s)\n", dias);

    return 0;
}
